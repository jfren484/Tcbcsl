@model ChurchListModel
@{
    const string title = "Churches";
    ViewBag.Title = title + Model.Year.AsTitleSuffix();
}
<partial name="_YearPartial" />
<h2>@title in our League</h2>
<div class="row">
    @Html.DisplayFor(m => m.Churches)
</div>