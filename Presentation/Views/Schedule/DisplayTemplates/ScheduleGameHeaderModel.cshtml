@model ScheduleGameHeaderModel
@(Model.DisplayOutcome
    ? Model.Outcome
    : Model.DisplayLocation
        ? Model.Location
        : Model.GameDate.ToString("t") + (string.IsNullOrEmpty(Model.Location) ? "" : " - " + Model.Location))
